- name: Deploy Docker Compose
  hosts: all
  tasks:
  
    - name: Copy stage docker-compose file to remote server
      copy:
        src: {{ ci_dir }}/ansible/stage/docker-compose.yaml
        dest: ~/docker-compose.yaml

    - name: Run docker-compose up
      command: docker-compose up -d
      args:
        chdir: ~/